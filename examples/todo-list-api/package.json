{
  "name": "prism-backend-example-todo-list-api",
  "description": "A todo list API example with Prism",
  "version": "0.0.4",
  "scripts": {
    "restart": "npm stop && npm start",
    "start": "mongod --fork --syslog && node start",
    "stop": "pkill mongod",
    "test": "node test"
  },
  "dependencies": {
    "fire-backend-test": "^1.0.2"
  }
}
